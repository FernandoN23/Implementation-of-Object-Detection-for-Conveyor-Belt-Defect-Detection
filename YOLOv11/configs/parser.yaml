# ==============================================================
# Departamento de Ingeniería Mecánica - Universidad de Chile
# Trabajo de Memoria de Título:
# "Implementación de algoritmos de reconocimiento de objetos
#  para la identificación de fallas en correas transportadoras"
# Autor: Fernando N.
# --------------------------------------------------------------
# Archivo: parser.yaml
# Configuración global del proyecto YOLOv11 para la lectura de
# rutas, recursos y parámetros de ejecución.
# ==============================================================

project_name: yolov11
task: detect
model: configs/yolo11.yaml
data: configs/dataset.yaml

paths:
  root_dir: ${oc.env:PROJECT_ROOT, .}
  data_dir: ${paths.root_dir}/data
  runs_dir: ${paths.root_dir}/runs
  weights_dir: ${paths.root_dir}/weights
  log_dir: ${paths.root_dir}/logs
  results_dir: ${paths.root_dir}/results

device: 0
imgsz: 640
batch: 8
epochs: 100
workers: 8
seed: 42

# Variante base del modelo YOLOv11
scale: n  # opciones: [n, s, m, l, x]
